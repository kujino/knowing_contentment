<turbo-stream target="reaction_button_<%= @post.id %>" action="replace">
  <template>
    <%= render partial: 'posts/reaction_buttons',
               formats: [:html],
               locals: { post: @post } %>
  </template>
</turbo-stream>
