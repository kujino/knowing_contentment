<%= link_to reanction_path(current_user.reactions.find_by(post_id: post.id)), id: "reaction-button-for-post-#{post.id}", data: { turbo_method: :delete } do
  <%= image_tag "unreaction.svg" %>
<% end %>